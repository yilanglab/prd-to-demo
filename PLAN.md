# PRD to Demo Skill V1 计划

## 目标

构建一个可复用的 `prd-to-demo` Skill，将 DOCX、PDF、Markdown、图片或混合来源的 PRD，经过事实追踪、产品建模和两次人工确认，转化为可运行、可验证的高保真前端交互 Demo。Skill 固化工作方法与质量门槛，不把某个项目的页面或结论写死。

## 适用范围

- 内部管理平台、运营后台、工作台和复杂业务工具。
- 需要把 PRD 快速转化为可汇报、可体验、可供产品与研发评审的交互原型。
- 从空目录初始化前端 Demo，或在已有 React 项目中补充原型页面。
- 允许用 Mock 数据和前端状态表达暂未建设的后端能力。

## 不适用范围

- 不以静态效果图代替交互 Demo。
- 不默认实现生产级后端、数据库、鉴权、消息队列或部署流水线。
- 不在没有产品合同的情况下直接从 PRD 生成页面。
- 不替代产品、研发、安全或合规负责人对关键业务规则的确认。
- 不盲目重新初始化已有项目，也不覆盖已有代码和用户改动。

## 输入

- 原始 PRD：`.docx`、`.pdf`、`.md`、`.mdx`、图片或其组合。
- 可选的补充材料：流程图、截图、竞品、品牌规范、接口说明和已有分析文档。
- 可选的已有前端项目与工程约束。
- 用户指定的产品边界、目标受众、演示场景和交付期限。

## 输出

- 产品合同：`PRODUCT.md`。
- 设计与实现约束：`DESIGN.md`。
- PRD 事实到页面、状态和交互的追踪表：`PRD-TRACEABILITY.md`。
- 待确认问题及决策记录：`OPEN-QUESTIONS.md`。
- 面向汇报和走查的演示脚本：`DEMO-SCRIPT.md`。
- 使用 Mock 数据的高保真前端交互 Demo。
- lint、typecheck、test、build、浏览器 Smoke 和未验证项记录。

## 六阶段流程

两道人工确认门禁必须顺序通过，默认不得合并；未获得门禁 1 对产品合同的明确确认前，不得进入阶段 3 或输出其原型计划材料。

### 阶段 1：输入盘点

建立来源清单，区分原始事实、已有分析和视觉参考；记录来源优先级、版本、可读性与缺失信息。只读检查已有项目，确认技术栈、脚本、组件和用户资产。

### 阶段 2：PRD 解析与产品建模

提取产品边界、角色、核心对象、生命周期、规则、权限、后台动作和异常分支。每条信息标为 `PRD 明确`、`基于 PRD 推导`、`Demo Mock` 或 `待产品确认`，并建立稳定追踪 ID。

**人工确认门禁 1：产品合同。** 用户确认产品范围、关键对象、生命周期、角色权限、核心规则和待确认项后，才进入原型规划。用户说“先讨论”时必须停在这里，不得初始化或修改产品代码。

### 阶段 3：原型范围与体验规划

把产品模型转化为信息架构、关键用户任务、端到端流程、页面清单、状态矩阵、P0/P1/P2 范围、Mock 数据计划和演示叙事。每个 P0 任务必须能从入口走到可验证结果。

**人工确认门禁 2：原型计划。** 用户确认页面范围、P0 主链路、状态覆盖、Mock 边界和演示故事后，才进入设计与研发。需求变化返回对应门禁重新确认。

### 阶段 4：设计基础与组件规划

定义语义色彩、字阶、间距、圆角、阴影、层级、内容宽度、响应式和交互反馈。盘点现有组件，记录复用、安装、组合或自研决策，并建立页面到组件的映射。

### 阶段 5：前端实现

按 P0 主链路纵向实现，再补 P1/P2。所有可见入口必须有结果或明确禁用原因；未接真实后端的动作必须用一致的 Mock 状态表达。实现过程中持续维护追踪表，禁止用代码反向篡改已确认的产品事实。

### 阶段 6：验证与交付

验证主链路、状态、权限、响应式、键盘、焦点、可访问名称、横向溢出和控制台错误；运行项目真实的 lint、typecheck、test、build、diff check 和浏览器 Smoke。缺失脚本或无法执行的检查必须标为“未验证”，不得伪造通过。最终交付 Demo、文档、验证证据和已知缺口。

## 不可变技术栈

- React + TypeScript。
- shadcn/ui，新项目采用 Base UI 路线的 `base-nova`。保留既有代码，但所有新 Demo 页面必须使用 Base UI-backed 组件；若现有 shadcn 不是 Base UI 路线，先记录隔离或迁移计划，禁止覆盖式重新初始化。
- `@base-ui/react` 提供行为 Primitive、焦点管理和无障碍语义。
- Tailwind CSS 消费语义 Token，不在业务页面散落硬编码颜色。
- Lucide 是常规产品图标的唯一来源，不使用 Emoji 充当 UI 图标。

组件决策顺序固定为：现有 `components/ui` → shadcn registry 或 dry-run 检查 → Base UI Primitive 组合 → 仅在前三者都不能满足时自研。不得用 `div + click` 重写已有交互控件。

## 验收标准

- 每条 P0 需求都能追踪到页面、状态、交互与验证证据。
- P0 主任务可完整执行，所有入口有反馈，无死按钮和错误路由。
- 正常、空、加载、错误和无权限状态已覆盖或明确标记不适用。
- 桌面、平板、移动端和宽屏均完成核对，无整页横向溢出。
- 键盘顺序、焦点进入与恢复、可访问名称符合组件语义。
- 无未解释的控制台运行时异常。
- 项目存在并实际运行 lint、typecheck、test、build；缺项必须作为未验证项交付。
- 浏览器 Smoke 覆盖首页、P0 主链路、关键弹层和主要状态。
- Skill 自身通过结构校验、自测和确定性脚本验证。

## 研发与验证阶段

1. 初始化 Skill 结构及代理元数据。
2. 编写核心流程、按需参考资料和可复制模板。
3. 实现项目盘点与交付物校验脚本。
4. 用临时目录覆盖完整成功案例与失败案例。
5. 运行 Skill 结构校验、脚本自测、静态残留检查和格式检查。
6. 使用不同类型的真实 PRD 做后续前向测试，再根据实际偏差迭代。

## V1 不做什么

- 不附带固定业务领域的 React 页面模板，避免把 SkillForge 等单一项目结构传播到新项目。
- 不自动安装全局 Skill、依赖或外部服务。
- 不自动提交、推送、部署或修改生产系统。
- 不保证生成真实后端、真实权限或持久化数据。
- 不绕过两次人工确认门禁。
- 不把视觉风格固定为某个品牌；只固定设计基础和技术实现的质量边界。
